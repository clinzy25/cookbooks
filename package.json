{
  "name": "cookbooks",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "concurrently \"pnpm i --prefix client\" \"pnpm i --prefix server\"",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "migrate:up": "knex migrate:latest --knexfile server/src/db/knexfile.js",
    "migrate:down": "knex migrate:down --knexfile server/src/db/knexfile.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
